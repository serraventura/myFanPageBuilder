import * as React from "react";
import Builder from "../containers/builder";

const App = (props) => (
    <div>
        <h1>Hello W!</h1>
        <Builder></Builder>
    </div>
);

export default App;